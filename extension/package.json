{
  "name": "like-button-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/content.ts src/background.ts src/pow-worker.ts --bundle --outdir=dist --format=esm --outbase=src && cp manifest.json dist/",
    "dev": "npm run build -- --watch"
  }
}
